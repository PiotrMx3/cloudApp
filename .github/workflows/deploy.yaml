on:
  push:
    branches: [main]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

  set-node-test:
    steps:
      - uses: actions/setup-node@v6
      - run: npm ci
      - run: npm test
